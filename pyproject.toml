[project]
name = "nonebot-plugin-crypto"
version = "0.1.0"
description = "基于 Binance API 的 NoneBot 加密货币行情插件"
readme = "README.md"
requires-python = "==3.10.*"
authors = [{ name = "ByteColtX", email = "umk@live.com" }]
dependencies = [
  "httpx>=0.27.0,<1.0.0",
  "nonebot-adapter-onebot>=2.4.6,<3.0.0",
  "nonebot2>=2.5.0,<3.0.0",
]

[project.urls]
Homepage = "https://github.com/ByteColtX/nonebot-plugin-crypto"
Issues = "https://github.com/ByteColtX/nonebot-plugin-crypto/issues"
Repository = "https://github.com/ByteColtX/nonebot-plugin-crypto.git"

[dependency-groups]
dev = [
  "bump-my-version>=1.2.7",
  "nonebot2[fastapi]>=2.5.0,<3.0.0",
  "nonebug>=0.4.4,<1.0.0",
  "poethepoet>=0.42.1",
  "pytest>=9.0.2,<10.0.0",
  "pytest-asyncio>=1.3.0,<1.4.0",
  "pytest-cov>=7.0.0",
  "pytest-xdist>=3.8.0,<4.0.0",
  "ruff>=0.15.2,<1.0.0",
]

[build-system]
requires = ["uv_build>=0.10.0,<0.13.0"]
build-backend = "uv_build"

[tool.nonebot]
plugins = ["nonebot_plugin_crypto"]

[tool.poe.tasks]
test = "pytest --cov=src --cov-report=xml --junitxml=./junit.xml -n auto"
bump = "bump-my-version bump"
show-bump = "bump-my-version show-bump"

[tool.bumpversion]
current_version = "0.1.0"
commit = true
message = "release: 将版本从 {current_version} 更新至 {new_version}"
tag = true

[[tool.bumpversion.files]]
filename = "uv.lock"
search = "name = \"nonebot-plugin-crypto\"\nversion = \"{current_version}\""
replace = "name = \"nonebot-plugin-crypto\"\nversion = \"{new_version}\""

[tool.pytest]
pythonpath = ["src"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.10"
typeCheckingMode = "standard"

[tool.ruff]
line-length = 88
src = ["src", "tests"]
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "ASYNC", "C4", "PT", "RUF"]
ignore = ["E402", "UP037", "RUF001", "RUF002", "RUF003"]
